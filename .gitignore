node_modules
dist

# Rendered output. Videos you import as source belong in public/ instead.
out

# Remotion Studio and bundler caches
.remotion

# Local transcription working directory: whisper.cpp build and models
.cache

# Transcripts derived from narration tracks (regenerate with `npm run transcribe`)
*.transcript.json

.DS_Store
.env
.env.local
*.log

# HyperFrames local render cache
hyperframes-showreel/out
hyperframes-showreel/.hyperframes
hyperframes-showreel/node_modules
hyperframes-showreel/snapshots
